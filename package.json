{
  "name": "photo_slide_galery",
  "version": "1.0.0",
  "description": "gallery",
  "main": "yes",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "rm -rf docs && mkdir docs && cp -r * docs && rm -rf docs/node_modules docs/package.json docs/package-lock.json"
  },
  "author": "",
  "license": "ISC"
}
